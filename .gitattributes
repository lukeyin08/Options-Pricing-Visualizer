# The plotly figures in figures/ are generated by scripts/generate_figures.py.
# Mark them as generated so GitHub Linguist excludes them from the language
# stats; otherwise the large HTML files dominate over the Python source.
figures/*.html linguist-generated=true
