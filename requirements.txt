# Pinned for reproducibility. Python 3.11+ recommended (developed/tested on 3.10+).
#
# --- Core runtime ---------------------------------------------------------
# scipy is deliberately NOT a runtime dependency: the pricing, Greeks, and the
# standard normal distribution are all implemented from first principles.
numpy==2.2.6
matplotlib==3.10.9
plotly==6.8.0

# --- Test only ------------------------------------------------------------
# scipy appears here only to validate the from-scratch implementation.
pytest==9.1.0
scipy==1.15.3

# --- Optional bonus dashboard --------------------------------------------
streamlit==1.58.0
